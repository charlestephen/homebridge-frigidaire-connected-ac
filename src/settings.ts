/**
 * Platform name users register in the Homebridge config.json (must match the
 * pluginAlias in config.schema.json).
 */
export const PLATFORM_NAME = 'FrigidaireACPlatform';

/**
 * Must match the "name" field in package.json.
 */
export const PLUGIN_NAME = 'homebridge-frigidaire-ac';
